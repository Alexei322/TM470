import { createTheme } from "@mui/material/styles";

const formTheme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#FF90BC",
    },
    secondary: {
      main: "#EF476F",
    },
    text: {
      primary: "#2F0A28",
    },
  },
});

export default formTheme;
